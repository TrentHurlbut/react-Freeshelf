import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import FreeshelfBase from './FreeshelfBase';

ReactDOM.render(
    <React.StrictMode>
      <FreeshelfBase />
    </React.StrictMode>,
  document.getElementById('root')
);